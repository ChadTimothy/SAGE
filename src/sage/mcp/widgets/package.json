{
  "name": "@sage/chatgpt-widgets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:checkin": "WIDGET=checkin vite build",
    "build:progress": "WIDGET=progress vite build",
    "build:graph": "WIDGET=graph vite build",
    "build:practice": "WIDGET=practice vite build",
    "build": "tsc && rm -rf dist && npm run build:checkin && npm run build:progress && npm run build:graph && npm run build:practice",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx",
    "test": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vis-network": "^9.1.9",
    "vis-data": "^7.1.9"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "typescript": "^5.4.5",
    "vite": "^5.3.1",
    "vite-plugin-singlefile": "^2.0.1",
    "vitest": "^1.6.0",
    "@testing-library/react": "^15.0.7",
    "jsdom": "^24.1.0"
  }
}
